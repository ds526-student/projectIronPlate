<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProjectIronPlate" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/secondary</item>
        <item name="colorOnPrimary">@color/primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary</item>
        <item name="colorSecondaryVariant">@color/secondary</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/primary</item>
        <!-- Bottom Navigation styling -->
        <item name="bottomNavigationStyle">@style/Widget.ProjectIronPlate.BottomNavigationView</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Custom Bottom Navigation View Style -->
    <style name="Widget.ProjectIronPlate.BottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <item name="materialThemeOverlay">@style/ThemeOverlay.ProjectIronPlate.BottomNavigation</item>
    </style>

    <!-- Theme overlay for Bottom Navigation -->
    <style name="ThemeOverlay.ProjectIronPlate.BottomNavigation" parent="">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorOnSurface">@color/text_secondary</item>
    </style>
</resources>